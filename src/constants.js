/**
 * constants.js
 *
 * Hold re-usable constants for the app.
 */

const LOCAL_STORAGE_KEY = 'TASKVOLT';

const TYPES = {
  TODO: 'TODO',
  LIST: 'LIST',
  PROJECT: 'PROJECT'
};

export { LOCAL_STORAGE_KEY, TYPES };
