/**
 * jss/styles.js
 *
 * Holds re-usable style objects for jss stlying.
 */

const flex = {
  display: 'flex'
};

const fullWidth = {
  width: '100%'
};

const hidden = {
  display: 'none'
};

export { flex, fullWidth, hidden };
