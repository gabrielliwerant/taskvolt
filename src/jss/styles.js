/**
 * jss/styles.js
 *
 * Holds re-usable style objects for jss stlying.
 */

const flex = {
  display: 'flex'
};

const flexCenterY = {
  display: 'flex',
  alignItems: 'center'
};

const fullWidth = {
  width: '100%'
};

const hidden = {
  display: 'none'
};

export { flex, flexCenterY, fullWidth, hidden };
